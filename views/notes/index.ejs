<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <title>Notes</title>
        <link rel="stylesheet" href="/style.css">
    </head>

    <body class="container">
        <h1>All Notes</h1>
        <div>
            <span class="text-secondary">
                <a href="/">Home </a> >
                <a href="#">Notes </a>
            </span>
        </div>
        <div class="d-flex form border-0">
            <form id="searchform" action="/notes" method="GET" class="">
                <label for="search">Search</label>
                <input type="search"id="searchKey"  name="searchKey" 
                    value="<%= searchKey %>"
                    placeholder="Search key..."
                    class="form">
                <button type="submit" class="btn">🔍 Search</button>
            </form>
            <a href="/notes/create" class="btn mx-1 btn-a">Create Note</a>
        </div>
        <ul>
        <%  notes.forEach(note => { %>
            
            <li>
                <h2> <%= note.title %> </h2>
                <% if(note.contents) { %> <p><%= note.contents.substring(0, 200) %>...</p> <%}%>
                <small><%= new Date(note.created_at) %></small>
                <a href="/notes/<%= note.id %>">View More</a>
            </li>
        <% }) %>
        </ul>
    </body>
</html>
